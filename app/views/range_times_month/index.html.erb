<p id="notice"><%= notice %></p>

<div class="box-container">
  <h1>Mês</h1>

  <table class="table">
    <thead class="thead-default">
      <tr>
        <th>Data</th>
        <th>Tempo</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @total_seconds.each do |t| %>
        <tr>
          <td><%= t.day.to_datetime.strftime("%d/%m/%Y") %></td>
          <td><%= RangeTimesHelper.humanize(t.total_seconds) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

