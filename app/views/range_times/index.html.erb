<p id="notice"><%= notice %></p>

<h1>Range Times</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Date</th>
      <th>Start time</th>
      <th>End time</th>
      <th>Difference hours</th>
      <th>Difference seconds</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @range_times.each do |range_time| %>
      <tr>
        <td><%= range_time.start_time.strftime("%d/%m/%Y") %></td>
        <td><%= range_time.start_time.strftime("%k:%M:%S") %></td>
        <td>
          <% if range_time.end_time.nil? %>
            <%= '-' %>
          <% else %>
            <%= range_time.end_time.strftime("%k:%M:%S") %>
          <% end %>           
        </td>
        <td>
          <% if range_time.end_time.nil? %>
            <%= '-' %>
          <% else %>
            <%= range_time.difference_hours.round(2) %></td>
          <% end %> 
        <td>
          <% if range_time.end_time.nil? %>
            <%= '-' %>
          <% else %>
            <%= range_time.difference_seconds.round(2) %>
          <% end %> 
        </td>   
        <td><%= link_to 'Show', range_time %></td>
        <td><%= link_to 'Edit', edit_range_time_path(range_time) %></td>
        <td><%= link_to 'Destroy', range_time, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    <tr>
        <td>Soma</td>
        <td></td>
        <td></td>
        <td>1</td>   
        <td>2</td>
        <td></td>
        <td></td>
        <td></td>
    </tr>
  </tbody>
</table>

<br>
<%= link_to "back", controller: "range_times", action: "new" %>

